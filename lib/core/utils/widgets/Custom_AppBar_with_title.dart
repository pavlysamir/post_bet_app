import 'package:flutter/material.dart';

class CustomAppbareWithTitle extends StatelessWidget {
  const CustomAppbareWithTitle({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar();
  }
}
